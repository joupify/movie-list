<h1>My Lists</h1>

<div class="row">
  <div class="col 3 p-3">
    <%= @lists.each do |list| %>
      <%= list.name %>
    <% end %>

    <%= link_to 'create a movie list', new_list_path(@list), class:"btn btn-flat" %>
  </div>
</div>
